# SOC 2 Remediation — Code-Change Effort Estimates & Status

**Scope:** Knowledge Cloud backend (Java/Spring) + frontend (Angular) + in-repo config/IaC  
**Basis:** a single developer familiar with the codebase  
**Code-fixed:** 20 / 90  ·  **Remaining code effort:** 37.25–66.75 dev-days  

> Generated by `build_report.js` from `report_base.json` + `estimates.js` + `fix_status.json`. Do not hand-edit; change those sources and rebuild.

## Headline (code only)

| Metric | Value |
|---|---|
| Total code effort (all 90) | **45–79 dev-days** |
| Code-fixed so far | 20 / 90 |
| Remaining (open) code effort | **37.25–66.75 dev-days** |
| Backend + Frontend only (71) | 37.25–65.75 dev-days |
| Critical + High, BE/FE only (30) | 17.5–31.25 dev-days |
| > 1 day (cross-cutting) | 16 — SEC-009, SEC-010, SEC-020, SEC-024, SEC-028, SEC-061, SEC-072, SEC-073, SEC-037, SEC-049, SEC-064, SEC-075, SEC-085, SEC-087, SEC-088, SEC-057 |
| "Discuss" (decide scope first) | 7 — SEC-010, SEC-024, SEC-061, SEC-085, SEC-087, SEC-088, SEC-059 |
| Separate non-code tasks | 72 (across 48 findings) |

## All 90 tasks

| ID | Status | Sev | Comp | Group | Code est. | Conf | Ops | Title |
|---|---|---|---|---|---|---|---|---|
| SEC-001 | Fixed | Critical | Backend | G3 | 0.5–1 | Medium | 3 | Hardcoded, low-entropy JWT signing secret committed in source enables token forgery and full privilege escalation |
| SEC-002 | Fixed | Critical | Backend | G1 | 0.25 | High | 3 | Live Git Personal Access Token hardcoded in committed config template (and live git password recoverable from Dockerfile history) |
| SEC-003 | Fixed | Critical | Backend | G1 | 0.25 | High | 3 | TLS keystore (server.jks) and DER private key committed to source control with plaintext keystore password; .gitignore inert |
| SEC-004 | Fixed | Critical | Backend | G1 | 0.25 | High | 2 | Hardcoded ShareFile production credentials and OAuth client secret committed in source |
| SEC-005 | Fixed | Critical | Backend | G9 | 0.25–0.5 | High |  | CORS allows any origin reflected with credentials (allowCredentials=true + allowedOriginPattern '*') |
| SEC-011 | Fixed | Critical | Backend | G13 | 0.5 | High |  | Full response bodies logged at INFO for every consumer API endpoint — reset tokens, JWTs & PII leak to log files |
| SEC-006 | Fixed | High | Backend | G8 | 0.5 | High | 1 | Outbound TLS certificate AND hostname validation fully disabled in Git, License, and container git clients (trust-all) |
| SEC-007 | Fixed | High | Backend | G10 | 0.5–1 | Medium |  | OS command injection via git CLI: repo name and release tags interpolated into a shell command stream |
| SEC-008 | Fixed | High | Backend | G10 | 0.25–0.5 | High |  | SOQL injection in Salesforce contact lookup (ticket email concatenated into query) |
| SEC-009 | Fixed | High | Backend | G5 | 1–2 | Medium |  | No object-level / tenant authorization on resource-by-id endpoints (IDOR), and the @PreAuthorize target (#targert) is misspelled so object checks are dead code |
| SEC-010 | Open | High | Backend | G3 | 1–2 | Discuss | 1 | Service-identity audiences grant fixed high privileges purely from a client-supplied JWT audience claim; refresh endpoint re-issues tokens from an unverified expired token |
| SEC-012 | Open | High | CI-CD | G2 | 0.5 | Medium | 2 | Pipeline packages git-tracked secrets (config dir) into the deploy artifact uploaded to S3 |
| SEC-013 | Open | High | Infra | G18 | 0.5–1 | Medium | 2 | Overly broad wildcard IAM policies (admin actions + iam:PassRole *) in CloudFormation pipeline IaC |
| SEC-014 | Open | High | Infra | G8 | 0.5 | Medium | 1 | Edge reverse proxy serves only plaintext HTTP (no TLS, no HTTP->HTTPS redirect) and proxies upstreams over http:// |
| SEC-015 | Fixed | High | Backend | G8 | 0.25 | High | 1 | PostgreSQL connection uses NonValidatingFactory — TLS without certificate validation (DB MITM) |
| SEC-016 | Open | High | Backend | G12 | 0.25–0.5 | Medium | 1 | Jasypt configured with weak PBEWithMD5AndDES algorithm and low iteration count for secrets at rest |
| SEC-017 | Fixed | High | Backend | G12 | 0.25–0.5 | High |  | Security tokens and package passwords generated with non-cryptographic PRNG (RandomStringUtils / java.util.Random) |
| SEC-018 | Fixed | High | Backend | G20 | 0.5 | High | 1 | Actuator endpoints permitAll (no auth); dead management.security.enabled=false and static admin/secret credentials committed across all environments |
| SEC-019 | Open | High | Backend | G15 | 0.5–1 | Medium | 1 | No account lockout / brute-force throttling or rate limiting on login, password-reset, or any endpoint |
| SEC-020 | Open | High | Frontend | G4 | 1–2 | Medium |  | JWT access token, 30-day refresh token, and user PII persisted to browser localStorage (XSS-exfiltratable) |
| SEC-021 | Open | High | Frontend | G20 | 0.25–0.5 | High |  | nb-markdown renders user-authored markdown to innerHTML WITHOUT sanitization on the readonly path (stored DOM XSS) |
| SEC-022 | Fixed | High | Backend | G20 | 0.25 | High |  | S3 pre-signed download URLs hardcoded to 6-hour validity, ignoring configured link-expiry policy |
| SEC-023 | Fixed | High | Backend | G1 | 0.25–0.5 | High | 1 | Hardcoded GitHub webhook HMAC secret 'netbrain', non-constant-time comparison, and signature logging |
| SEC-024 | Open | High | Backend | G16 | 1–3 | Discuss | 1 | fastjson 1.2.83 (EOL) and EOL/vulnerable frontend production dependencies shipped to production |
| SEC-025 | Open | High | CI-CD | G17 | 0.5–1 | Medium | 1 | No SAST quality gate, SCA, or secret scanning in either CI pipeline |
| SEC-026 | Open | High | CI-CD | G17 | 0.5–1 | Medium | 2 | Deployment compose pulls application images from personal Docker Hub namespaces; EOL/unpinned base images; containers run as root |
| SEC-027 | Open | High | Backend | G19 | 0.5–1 | Medium | 1 | Customer support-ticket PII has no enforced deletion/retention despite a 'delete when resolved' flag; expired tokens never purged |
| SEC-028 | Open | High | Backend | G14 | 1–2 | Medium | 1 | Security events not audited: failed logins, authorization denials, logout, and all administrative IAM changes are unrecorded |
| SEC-029 | Open | High | Infra | G14 | 0.5–1 | Medium | 3 | No centralized/tamper-resistant log aggregation; audit log is a mutable JPA entity |
| SEC-030 | Fixed | High | Backend | G13 | 0.25 | High | 1 | Live Salesforce OAuth access token and customer license keys logged in clear text (production paths) |
| SEC-031 | Open | High | Backend | G15 | 0.25–0.5 | Medium | 2 | No application-level rate limiting / login throttling enables DoS and brute-force (availability) |
| SEC-061 | Open | High | Backend | G6 | 3–5 | Discuss | 1 | No multi-factor authentication (MFA) for any user, including the built-in admin |
| SEC-062 | Fixed | High | Backend | G5 | 0.25 | High |  | Central RBAC permission check uses substring match (contains) instead of equality, allowing privilege over-grant |
| SEC-072 | Fixed | High | Backend | G5 | 1–2 | Medium |  | Custom object-level permission evaluator ignores the target object, defeating per-tenant authorization application-wide |
| SEC-073 | Open | High | Backend | G5 | 1–1.5 | Medium |  | Per-license resource/package endpoints authorize on coarse permission only, allowing cross-tenant package and resource download |
| SEC-074 | Open | High | Backend | G5 | 0.5–1 | High |  | Deployment logs and download links retrievable by any authenticated caller via unscoped deploymentId (cross-tenant disclosure) |
| SEC-076 | Fixed | High | CI-CD | G17 | 0.25–0.5 | High | 2 | No enforced peer review on code changes: no CODEOWNERS, no/empty PR template, branch protection unconfigured |
| SEC-032 | Open | Medium | Infra | G2 | 0.25–0.5 | High | 2 | Plaintext DB passwords ('Netbrain!', postgres) hardcoded in docker-compose files and packaged application.properties |
| SEC-033 | Open | Medium | Backend | G1 | 0.25 | High | 1 | Jasypt master passwords committed next to their ciphertext in test resources/code |
| SEC-034 | Open | Medium | Backend | G7 | 0.25–0.5 | High |  | Password complexity enforced only client-side; backend accepts any password on create/update/reset |
| SEC-035 | Open | Medium | Backend | G7 | 0.5 | Medium |  | Password-reset flow enables user/email enumeration and tokens are not single-use on verify (PII disclosed on public verify) |
| SEC-036 | Fixed | Medium | Backend | G5 | 0.25–0.5 | High |  | Write/sync operations lack method-level authorization; permission check uses substring match instead of equality |
| SEC-037 | Open | Medium | Backend | G11 | 1–2 | Medium |  | Systemic missing request-body validation: @RequestBody without @Valid and DTO constraints commented out |
| SEC-038 | Open | Medium | Backend | G10 | 0.5 | High |  | Path traversal sinks: Zip Slip in CommonUtils.unzip and uploaded-filename path in framework-release upload |
| SEC-039 | Open | Medium | Backend | G8 | 0.25 | High |  | Custom SMTP send path enables TLS without enforcing certificate validation or requiring STARTTLS |
| SEC-040 | Open | Medium | Backend | G12 | 0.5 | Medium |  | Package signing only covers the first N bytes of the upgrade package (truncated integrity) |
| SEC-041 | Open | Medium | Backend | G20 | 0.5 | High | 1 | Unauthenticated actuator, API-docs/swagger, and webhook endpoints removed from the security filter chain (web.ignoring + permitAll) |
| SEC-042 | Open | Medium | Backend | G13 | 0.5–1 | High |  | Server-side root-cause exception messages returned to clients; verbose stack traces and printStackTrace() in production |
| SEC-043 | Open | Medium | Backend | G14 | 0.5–1 | Medium |  | Audited data-export/download actions attributed to licenseId/robot instead of the authenticated user; UI reads not audited |
| SEC-044 | Open | Medium | Backend | G15 | 0.25 | High | 1 | Unbounded 4GB multipart upload limit against a 25-connection pool enables resource-exhaustion DoS |
| SEC-045 | Open | Medium | Backend | G12 | 0.25 | High | 1 | S3 object uploads do not specify server-side encryption |
| SEC-046 | Open | Medium | Infra | G9 | 0.5–1 | Medium | 1 | Frontend nginx serves no Content-Security-Policy or security response headers (and app filter omits Permissions-Policy/frame-ancestors and skips the management port) |
| SEC-047 | Open | Medium | Backend | G1 | 0.25–0.5 | High |  | GitHub webhook DTO/data fixtures and seed data embed PII-shaped values, reused weak hashes, and a committed Jasypt master password |
| SEC-048 | Open | Medium | Backend | G16 | 0.5–1 | Medium |  | Outdated backend libraries shipped to production (gson 2.8.5, httpcore/httpclient 4.x, reactor-core pin, saaj/jaxb) plus unpinned RELEASE metaversion |
| SEC-049 | Open | Medium | CI-CD | G17 | 1–2 | Medium | 2 | CI build/change-management gaps: tests skipped, unsigned artifacts, npm ci --force, OIDC token in world-readable /tmp, reused credential, empty prod approver |
| SEC-055 | Open | Medium | Frontend | G5 | 0.25–0.5 | Medium |  | Frontend authorization (route guard + nb-permissions + tenant interceptor) is UI-only and client-trusted |
| SEC-063 | Open | Medium | Backend | G5 | 0.25–0.5 | Medium |  | Hardcoded role-id '10000' approver bypass keyed to the all-permission built-in Admin role |
| SEC-064 | Open | Medium | Backend | G4 | 1–1.5 | Medium |  | Access-token 30-day default (config drift), no idle/session timeout, and no server-side token revocation |
| SEC-066 | Open | Medium | Backend | G4 | 0.5–1 | Medium |  | Offboarding/deactivation does not revoke outstanding tokens; deactivated users retain access until token expiry |
| SEC-068 | Open | Medium | Infra | G14 | 0.25–0.5 | Medium | 2 | Audit-relevant logs roll off the host within ~100 days; no enforced >=1-year retention |
| SEC-069 | Open | Medium | Backend | G8 | 0.25 | High | 2 | Application-tier TLS does not enforce a minimum protocol version or cipher suite (legacy TLS 1.0/1.1 negotiable) |
| SEC-070 | Open | Medium | Backend | G12 | 0.5–1 | Medium |  | Customer upgrade-package password stored in cleartext in the database (Deployment.password) |
| SEC-071 | Open | Medium | Backend | G7 | 0.5 | High |  | Password-reset tokens stored unhashed at rest (plaintext bearer secret in DB) |
| SEC-075 | Open | Medium | Backend | G5 | 1–2 | Medium | 2 | Customer (tenant) soft-delete is not enforced on reads and triggers no cascade purge of tenant data |
| SEC-077 | Open | Medium | CI-CD | G17 | 0.25–0.5 | High | 2 | Production deployment approval gates only the artifact upload, not the CodeDeploy deploy; non-release branches deploy with no approval |
| SEC-078 | Open | Medium | CI-CD | G17 | 0.5–1 | Medium | 1 | Deployed artifacts are not traceable to a source commit/version (static SNAPSHOT jar, overwrite S3 keys, no build metadata) |
| SEC-079 | Open | Medium | Infra | G18 | 0.25–0.5 | Medium | 3 | Rollback limited to hard deploy-failure; no immutable previous artifact and no health-based rollback |
| SEC-080 | Open | Medium | Infra | G18 | 0.5 | Medium | 3 | Production CodeDeploy deployment group ships with CloudWatch alarms disabled and no availability alerting wired anywhere |
| SEC-081 | Open | Medium | CI-CD | G17 | 0.25–0.5 | High |  | CodeDeploy 'ValidateService' health check only verifies the OS service is registered, not that the app serves traffic |
| SEC-083 | Open | Medium | CI-CD | G17 | 0.25 | High | 1 | CI/CD design document advertises a SonarQube security quality gate the pipelines do not enforce (control-design misrepresentation) |
| SEC-085 | Open | Medium | Backend | G19 | 1–2 | Discuss | 1 | No data-subject access / export / portability capability for personal data (GDPR Art.15/20, CCPA right to know) |
| SEC-086 | Open | Medium | Backend | G19 | 0.5–1 | Medium |  | User-account erasure is incomplete: deletion leaves orphaned PII-linked reset-password tokens and propagates no downstream deletion |
| SEC-087 | Open | Medium | Backend | G19 | 1–2 | Discuss | 1 | No consent capture or privacy notice at any personal-data collection point (operator accounts and support-ticket contacts) |
| SEC-088 | Open | Medium | Backend | G19 | 1–2 | Discuss | 1 | No data-classification or sensitivity-labeling scheme for confidential assets (license keys, ticket PII, customer network diagnostics) |
| SEC-089 | Open | Medium | Backend | G19 | 0.5–1 | Medium | 1 | Soft-delete-only for confidential customer (IE-instance) data with no retention-based purge job - data retained indefinitely |
| SEC-090 | Open | Medium | Backend | G19 | 0.5–1 | Medium | 1 | No secure destruction lifecycle for confidential attachments in S3/ShareFile; ShareFile deleteItem is dead code, no S3 lifecycle/object-lock |
| SEC-067 | Open | Medium | Backend | G14 | 0.5–1 | Medium | 1 | User/role/permission CRUD endpoints emit zero audit records (privileged actions unaudited) |
| SEC-050 | Open | Low | Backend | G10 | 0.25 | High |  | ShareFile getItemByPath builds URL with unencoded user path (URL/parameter injection) |
| SEC-051 | Open | Low | Backend | G10 | 0.25 | High |  | Templated support email injects unescaped user-controlled values (HTML/email content injection) |
| SEC-052 | Open | Low | Backend | G12 | 0.5 | Medium |  | Weak/legacy cryptography: MD5 file hashing and hand-rolled RSA license signature verification |
| SEC-053 | Open | Low | Backend | G2 | 0.25 | High |  | RSA license-verification public key embedded in committed config template alongside live secrets |
| SEC-054 | Open | Low | Frontend | G20 | 0.5–1 | Medium |  | Frontend defense-in-depth gaps: bearer token attached to any host, open-redirect returnUrl, untrusted content via [innerHTML], guid() uses Math.random() |
| SEC-056 | Open | Low | Backend | G13 | 0.25–0.5 | High |  | PII (email/displayName) and customer license data written to local application logs |
| SEC-057 | Open | Low | Frontend | G16 | 1–3 | Medium |  | Vulnerable/EOL frontend test & build tooling and transitive dev dependencies |
| SEC-058 | Open | Low | Backend | G16 | 0.5–1 | High |  | Very old test-scope libraries pinned across modules (Mockito 2.23.4; nb-kc-main JUnit 4.12 = CVE-2020-15250) |
| SEC-082 | Open | Low | Infra | G18 | 0.25 | High | 1 | Application, frontend, and reverse-proxy containers have no restart policy and no HEALTHCHECK (no runtime self-healing) |
| SEC-084 | Open | Low | CI-CD | G17 | 0.25 | High |  | SonarQube scanner configuration neuters its own SAST coverage: Java-binaries-only, no sonar.sources, excludes resources/ (where committed secrets live) |
| SEC-065 | Open | Low | Backend | G20 | 0.25–0.5 | Medium |  | Unwired license-key authentication provider has a no-op credential check (isValidLicense hardcoded true) |
| SEC-059 | Open | Info | Backend | G8 | 0.25 | Discuss | 1 | Inconsistent/undocumented mTLS boundary configuration across profiles |
| SEC-060 | Open | Info | Backend | G1 | 0.25 | High | 1 | Trivial credential-like literals and onboarding screenshots committed (informational; no live secrets) |

## Per-task detail

### SEC-001 — Hardcoded, low-entropy JWT signing secret committed in source enables token forgery and full privilege escalation

- **Critical / Backend** · SOC 2 CC6.1 · Group G3 JWT & token integrity
- **Status:** Fixed (2026-06-30, fb8e2d92) — Code: removed the hardcoded JWT HMAC secret literal ('netbrain-knowledge-cloud') and 'do not modify' comment from JwtToken; signing key now sourced from config property jwt.secret via new JwtTokenConfigurer (@PostConstruct -> JwtToken.initAlgorithm), no default so a missing key fails fast. Kept HMAC256 (symmetric externalize); asymmetric RS256/ES256 is a larger optional follow-up. Local dev key in gitignored application-local.properties. PREREQUISITE/DevOps backlog: add a random >=256-bit jwt.secret per environment to AWS Secrets Manager BEFORE deploying (app won't start without it), rotate to invalidate all existing forged/old tokens (coordinated redeploy), and purge the old literal from git history.
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: read the signing key from config/env instead of the hardcoded literal; optionally switch to RS256/ES256 (changes the sign/verify calls). Small if symmetric-externalize only; ~1d if you also do asymmetric.
- **Non-code tasks (handled separately):**
    - Generate a >=256-bit key per environment and store it in AWS Secrets Manager
    - Rotate the key and invalidate existing tokens (coordinated redeploy)
    - Purge the literal from git history

### SEC-002 — Live Git Personal Access Token hardcoded in committed config template (and live git password recoverable from Dockerfile history)

- **Critical / Backend** · SOC 2 CC6.1 · Group G1 Secrets removal & rotation
- **Status:** Fixed (2026-06-29, 296a023e) — Code: replaced hardcoded git.token in application.properties.template with jasypt encrypted placeholder (sourced from secret store); Dockerfile already free of git-credential echo. DevOps backlog: rotate kc PAT (shihui account already suspended), purge git history (secret still in commit 8a687a99), inject git auth from Secrets Manager.
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: replace the template token value with a placeholder, source from the secret store, and remove the RUN echo from the Dockerfile. Trivial edit.
- **Non-code tasks (handled separately):**
    - Revoke & rotate the Git PAT and the recoverable git password (treat as compromised)
    - Purge both from git history (BFG/filter-repo), force-push, all devs re-clone
    - Inject git auth at runtime from Secrets Manager

### SEC-003 — TLS keystore (server.jks) and DER private key committed to source control with plaintext keystore password; .gitignore inert

- **Critical / Backend** · SOC 2 CC6.1 · Group G1 Secrets removal & rotation
- **Status:** Fixed (2026-06-30) — Code/repo portion already resolved by the config restructure (develop merge), verified: server.jks + private_key.der are no longer git-tracked and not on disk; .gitignore now covers them (*.der, config/*.jks); no tracked file carries the plaintext keystore password (config/application.properties uses ${SERVER_SSL_KEY_STORE_PASSWORD}; keystore path/key delivered at runtime). No further developer change needed. DevOps backlog REMAINS: rotate the TLS cert/keypair + DER key + keystore password (still 'netbrain', confirmed live in Secrets Manager) and purge both files + the password from git history.
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: git rm --cached the tracked keystore/key so the existing .gitignore takes effect. Minimal repo change.
- **Non-code tasks (handled separately):**
    - Rotate the TLS cert/keypair and DER key (assume compromised), set a strong keystore password
    - Purge the files + password from git history
    - Provision the keystore/password at deploy time from Secrets Manager

### SEC-004 — Hardcoded ShareFile production credentials and OAuth client secret committed in source

- **Critical / Backend** · SOC 2 CC6.1 · Group G1 Secrets removal & rotation
- **Status:** Fixed (2026-06-30, 9e622a95) — Code: deleted the dead ShareFileV3.main() demo harness that hardcoded ShareFile creds (evan.bi / Netbrain1) + OAuth clientId/clientSecret; it was if(false) scaffolding with local C:\temp paths, not a prod entry point, and nothing referenced it. Secret values now absent from the working tree; module compiles. DevOps backlog: rotate the ShareFile account password + OAuth client secret (needs ShareFile admin) and purge both from git history.
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: remove the hardcoded ShareFile creds/OAuth secret, read from config. Trivial.
- **Non-code tasks (handled separately):**
    - Rotate the ShareFile account password and OAuth client secret (needs ShareFile admin)
    - Purge from git history

### SEC-005 — CORS allows any origin reflected with credentials (allowCredentials=true + allowedOriginPattern '*')

- **Critical / Backend** · SOC 2 CC6.1 · Group G9 Security headers / CORS / CSP
- **Status:** Fixed (2026-06-29, f26c8258) — Removed the CorsFilter (CorsConfig.java deleted) entirely instead of tightening it. SPA and API are same-origin in every env (prod/preprod/qa: same host under /api; dev: Angular dev-server proxies /api server-side); all other API callers are server-to-server. With no Access-Control-Allow-Origin emitted, browsers block all cross-origin requests by default - strictly stronger than an allowlist and eliminates the credentialed-wildcard CORS. Spring Security does not use .cors() and nothing referenced the bean.
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code: CorsConfig edit — drop the wildcard origin pattern, set allowCredentials(false), list explicit methods/headers. Verify each env SPA origin still works.

### SEC-011 — Full response bodies logged at INFO for every consumer API endpoint — reset tokens, JWTs & PII leak to log files

- **Critical / Backend** · SOC 2 CC7.2 · Group G13 Logging hygiene & PII redaction
- **Status:** Fixed (2026-06-29, d1674251) — Deleted APIInvokeLogAspect entirely (it existed only to dump request/response bodies, leaking JWT/reset-token/PII to logs). It was ad-hoc, scoped to just 5 consumer controllers, fired only on success (@AfterReturning - no failures/status), and carried a no-op @Around. No code referenced it. Uniform request/access logging + auth-failure monitoring should be handled by a real access log and the SEC-018/019 work, not this aspect.
- **Code estimate:** 0.5 dev-days · **Confidence:** High
- **Code work:** Code: remove the blanket response-body serialization in the logging aspect, log only method+status, add field masking and CRLF stripping. Central aspect, well-bounded.

### SEC-006 — Outbound TLS certificate AND hostname validation fully disabled in Git, License, and container git clients (trust-all)

- **High / Backend** · SOC 2 CC6.7 · Group G8 Encryption in transit (TLS)
- **Status:** Fixed (2026-07-02, ee7a0974) — Code: removed trust-all TLS from both outbound REST clients. RestClient (git) and LicenseServerRestClient (license) now use the default HttpClients.custom().build() which validates certificate chain + hostname via the JVM trust store; deleted the accepting TrustStrategy, NoopHostnameVerifier, and the dead turnOffSslChecking()/UNQUESTIONING_TRUST_MANAGER. Dockerfile already free of 'http.sslVerify false'; git CLI (GitCLIDiffer) has no SSL override. Compiles clean. DevOps backlog: if the internal git/license endpoints present private/self-signed certs, import that CA into the JVM/host truststore or these outbound calls will fail after this change.
- **Code estimate:** 0.5 dev-days · **Confidence:** High
- **Code work:** Code: remove the accepting TrustStrategy + NoopHostnameVerifier in the two REST clients, delete turnOffSslChecking(), remove http.sslVerify false from the Dockerfile.
- **Non-code tasks (handled separately):**
    - Import the internal/private CA into the JVM/dedicated truststore on the hosts

### SEC-007 — OS command injection via git CLI: repo name and release tags interpolated into a shell command stream

- **High / Backend** · SOC 2 CC6.1 · Group G10 Injection fixes
- **Status:** Fixed (2026-07-03, 0ca41a10) — GitCLIDiffer no longer pipes an interpolated command stream into a shell (/bin/sh or cmd.exe). Rewrote doSend() to run each git step via an explicit argv ProcessBuilder (no shell parsing): git init/add/commit, then git push -f <url> HEAD:diff-branches --push-option=<to> --push-option=<from> with stderr redirected to result.txt via ProcessBuilder.redirectError (replacing the shell '2>result.txt'); the 'echo >> README.md' shell redirect is replaced by a Java Files.writeString. Added allowlist validation (^[A-Za-z0-9._/-]+$, no '..') on repoName/from/to before use, rejecting shell metacharacters/newlines/path-traversal. Removed getCLI()/shell, buildCommands(), the commands field, and the PUSH_DIFF_CMD interpolation. Compiles clean; git command semantics preserved (init/add/commit/push refspec + push-options unchanged), so behavior is equivalent - full runtime regression of the diff flow needs the git server + pre-receive hook. NOTE: Consts.PUSH_DIFF_CMD is now an unused dead template, left in place.
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: invoke git via an explicit argv ProcessBuilder (no shell) and allowlist-validate repo/tag/branch names. Regression-test the diff/clone flow.

### SEC-008 — SOQL injection in Salesforce contact lookup (ticket email concatenated into query)

- **High / Backend** · SOC 2 CC6.1 · Group G10 Injection fixes
- **Status:** Fixed (2026-07-02, 1a55e365) — Code: SalesForceService.getContactId no longer concatenates the raw email into SOQL. Now escapes SOQL string-literal chars (backslash, single-quote) and URL-encodes the entire query via URLEncoder.encode(...UTF_8), so a crafted email cannot break out of the SOQL literal or inject extra query params. Compiles clean. Optional defense-in-depth not done: adding @Email + @Valid on the ticket-creation DTO/controller (would need spring-boot-starter-validation wired into netbrain-ticket); not required since the query building is now injection-safe.
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code: escape/encode the email in the SOQL (or use bind variables) + add @Email validation. Single method.

### SEC-009 — No object-level / tenant authorization on resource-by-id endpoints (IDOR), and the @PreAuthorize target (#targert) is misspelled so object checks are dead code

- **High / Backend** · SOC 2 CC6.1 · Group G5 Authorization & multi-tenancy
- **Status:** Fixed (2026-07-02, 077712f1) — Resolved by design decision (product owner): KC uses role/permission-level (RBAC) authorization - holding a permission grants access to all resources of that type; there is no per-tenant/object isolation requirement, so object-level checks (the IDOR concern) are intentionally out of scope. Cleanup done: replaced the misleading misspelled #targert (which silently resolved to null and implied a non-existent object check) with an explicit null across all 83 @PreAuthorize expressions, so annotations honestly express permission-only checks. Kept hasPermission (not hasAuthority) to preserve the CustomPermissionEvaluator's per-request deactivated-user block, which IS load-bearing: verified that validateByToken does NOT re-check isInactive for an already-issued token, so the evaluator's check is the per-request gate (deactivated users are also blocked at login via isValidUIToken). If per-tenant isolation is ever required, implement the 3-arg evaluator (hasPermission(id, type, perm)) + service-layer entitlement checks.
- **Code estimate:** 1–2 dev-days · **Confidence:** Medium
- **Code work:** Code: implement a real PermissionEvaluator receiving the target id (fix the #targert typo), enforce object/tenant entitlement in the service layer across the resource-by-id endpoints, add cross-tenant denial tests. Cross-cutting but bounded; shares the core with SEC-072.

### SEC-010 — Service-identity audiences grant fixed high privileges purely from a client-supplied JWT audience claim; refresh endpoint re-issues tokens from an unverified expired token

- **High / Backend** · SOC 2 CC6.3 · Group G3 JWT & token integrity
- **Status:** Open
- **Code estimate:** 1–2 dev-days · **Confidence:** Discuss
- **Code work:** Code (once the model is decided): derive the refreshed token identity from the verified refresh token and reject audience/subject mismatch; stop trusting a signature-only audience claim. Needs a decision on how services authenticate (per-service credential vs mTLS) before sizing.
- **Non-code tasks (handled separately):**
    - Decide & provision per-service credentials or mTLS for service identities

### SEC-012 — Pipeline packages git-tracked secrets (config dir) into the deploy artifact uploaded to S3

- **High / CI-CD** · SOC 2 CC6.1 · Group G2 Config & secret externalization
- **Status:** Open
- **Code estimate:** 0.5 dev-days · **Confidence:** Medium
- **Code work:** Code: Jenkinsfile change to stop bundling the config dir into the artifact; app already supports the prod profile sourcing from the secret store.
- **Non-code tasks (handled separately):**
    - Deliver runtime config/keystore via Secrets Manager/SSM at boot
    - Rotate key material once history is purged

### SEC-013 — Overly broad wildcard IAM policies (admin actions + iam:PassRole *) in CloudFormation pipeline IaC

- **High / Infra** · SOC 2 CC6.1 · Group G18 Infra/IaC, IAM & deploy resilience
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** IaC: edit the CloudFormation JSON to least-privilege actions, scope resource ARNs, and restrict iam:PassRole. Needs you to enumerate the pipeline's real actions.
- **Non-code tasks (handled separately):**
    - Add an IAM linter (cfn-nag/checkov) to the pipeline
    - Validate the scoped policy in a real deploy

### SEC-014 — Edge reverse proxy serves only plaintext HTTP (no TLS, no HTTP->HTTPS redirect) and proxies upstreams over http://

- **High / Infra** · SOC 2 CC6.7 · Group G8 Encryption in transit (TLS)
- **Status:** Open
- **Code estimate:** 0.5 dev-days · **Confidence:** Medium
- **Code work:** Config: add the TLS listener + 80->443 redirect + upstream-over-TLS in reverse_proxy.nginx.conf. Code/config side is small.
- **Non-code tasks (handled separately):**
    - Provision valid TLS certs and decide the production TLS-termination point

### SEC-015 — PostgreSQL connection uses NonValidatingFactory — TLS without certificate validation (DB MITM)

- **High / Backend** · SOC 2 CC6.7 · Group G8 Encryption in transit (TLS)
- **Status:** Fixed (2026-07-02) — No developer/code change: the JDBC URL is no longer committed anywhere in the repo (verified via git grep) - the config restructure moved spring.datasource.url into Secrets Manager. So the code/repo portion is resolved. DevOps backlog (finding NOT neutralized until done): the Secrets Manager datasource URL still contains sslmode=require&sslfactory=org.postgresql.ssl.NonValidatingFactory, so DB TLS is still unauthenticated (MITM). Fix in Secrets Manager per env: remove sslfactory=NonValidatingFactory and set sslmode=verify-full with sslrootcert=<AWS RDS CA bundle>, and place the RDS CA bundle on the hosts/containers.
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Config: remove sslfactory=NonValidatingFactory, set sslmode=verify-full + sslrootcert. One-line property change.
- **Non-code tasks (handled separately):**
    - Place the RDS CA bundle on the hosts

### SEC-016 — Jasypt configured with weak PBEWithMD5AndDES algorithm and low iteration count for secrets at rest

- **High / Backend** · SOC 2 CC6.1 · Group G12 Crypto at rest & weak crypto
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** Medium
- **Code work:** Code: change EncryptorConfig to PBEWITHHMACSHA512ANDAES_256 with high iterations + random IV. Small code change.
- **Non-code tasks (handled separately):**
    - Re-encrypt all stored secrets across environments and coordinate the rollout

### SEC-017 — Security tokens and package passwords generated with non-cryptographic PRNG (RandomStringUtils / java.util.Random)

- **High / Backend** · SOC 2 CC6.1 · Group G12 Crypto at rest & weak crypto
- **Status:** Fixed (2026-07-02, 5b522bb5) — Code: swapped non-crypto RandomStringUtils.random*() for the SecureRandom-backed RandomStringUtils.secure().next*() at all 4 security-token sites: password-reset token (AuthController), approval token (ApprovalToken), and the two customer-package passwords (AutoUpdateService). commons-lang3 3.17.0 (via Spring Boot 3.5 BOM) provides secure(). Kept the same charset/length (alphabetic/alphanumeric, 20 chars) so no format/DB-length change; only the RNG source changed from predictable to CSPRNG. Reset tokens already single-use (deleted after verify). Compiles clean.
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code: swap RandomStringUtils/java.util.Random for SecureRandom at the token/password sites; widen charset, keep tokens single-use. A few call sites.

### SEC-018 — Actuator endpoints permitAll (no auth); dead management.security.enabled=false and static admin/secret credentials committed across all environments

- **High / Backend** · SOC 2 CC6.1 · Group G20 Misc hardening
- **Status:** Fixed (2026-07-02, d6219947) — Code: (1) Added a dedicated actuator SecurityFilterChain (@Order(1), securityMatcher EndpointRequest.toAnyEndpoint()) that permits only /health (needed by the container HEALTHCHECK) and requires authentication for every other endpoint; removed the old .requestMatchers('/actuator/**').permitAll() from the main chain (now @Order(2)). (2) Locked web exposure to health ONLY (management.endpoints.web.exposure.include=health) with health show-details=never, so env/heapdump/configprops/beans/info are never exposed - info is excluded deliberately because build-info is generated and /actuator/info would otherwise disclose build/version metadata. (3) Removed the dead/misleading management.security.enabled, management.security.role, and (local-only) security.user.name/password props - no-ops in Spring Boot 3 (old Boot 1.x names) that only masqueraded as a control; the committed repo had no security.user.* (only the gitignored local file did). Compiles clean. DevOps backlog: bind the management port 8081 to localhost or drop the 8081 host mapping so actuator isn't reachable off-host.
- **Code estimate:** 0.5 dev-days · **Confidence:** High
- **Code work:** Code: dedicated authenticated actuator SecurityFilterChain, limit exposure to health/info, remove the static admin/secret, delete dead management.security.* props.
- **Non-code tasks (handled separately):**
    - Bind the management port to localhost or drop the 8081 host mapping (compose)

### SEC-019 — No account lockout / brute-force throttling or rate limiting on login, password-reset, or any endpoint

- **High / Backend** · SOC 2 CC6.1 · Group G15 Rate limiting & DoS resilience
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: add a LoginAttemptService / Bucket4j failed-attempt + lockout + backoff on /auth/* with tests. New but small mechanism; shares code with SEC-031.
- **Non-code tasks (handled separately):**
    - Optionally add nginx limit_req/limit_conn (config)

### SEC-020 — JWT access token, 30-day refresh token, and user PII persisted to browser localStorage (XSS-exfiltratable)

- **High / Frontend** · SOC 2 CC6.1 · Group G4 Token lifecycle & revocation
- **Status:** Open
- **Code estimate:** 1–2 dev-days · **Confidence:** Medium
- **Code work:** Code (front+back): server sets httpOnly+Secure+SameSite cookies (or keep tokens in-memory; exclude the token sub-state from the persisted NGXS key), plus CSRF handling. Genuine front/back integration — not a single-file change.

### SEC-021 — nb-markdown renders user-authored markdown to innerHTML WITHOUT sanitization on the readonly path (stored DOM XSS)

- **High / Frontend** · SOC 2 CC6.1 · Group G20 Misc hardening
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code: run DOMPurify.sanitize() on the readonly markdown path (mirror the editor path), centralize through one sanitize() helper, disable raw HTML in marked. Single component.

### SEC-022 — S3 pre-signed download URLs hardcoded to 6-hour validity, ignoring configured link-expiry policy

- **High / Backend** · SOC 2 C1.2 · Group G20 Misc hardening
- **Status:** Fixed (2026-06-30, e496af9b) — Externalized the S3 pre-signed URL TTL: AWSClient no longer hardcodes Duration.ofHours(6); it reads a new dedicated property application.package.downloadLink.presignedUrlExpireMinutes (default 360=6h), clamped to the valid S3 range [1min,7days]. NOTE: audit premise corrected - expireMinutes is the package *regeneration cadence* (short in dev/qa for fresh rebuilds, longer in prod), NOT the URL lifetime, so the two are intentionally separate and were NOT coupled (coupling would make dev URLs expire instantly). The real gap was the hardcoded/un-governed TTL; now configurable + documented, so C1.2 is satisfied. 6h retained as the deliberate customer download grace period; per-env override via ECS env var.
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: drive signatureDuration from the configured expireMinutes and clamp bad values. Single method.

### SEC-023 — Hardcoded GitHub webhook HMAC secret 'netbrain', non-constant-time comparison, and signature logging

- **High / Backend** · SOC 2 CC6.1 · Group G1 Secrets removal & rotation
- **Status:** Fixed (2026-07-02, 045d15af) — Removed the finding entirely by deleting the unused GitHubWebhookController (a prototype stub: it only verified the signature and returned ok/badRequest, triggered no downstream processing, and nothing referenced it). Also removed its now-dead '/webhooks' permitAll matcher in WebSecurityConfig. This eliminates the hardcoded 'netbrain' HMAC secret, the non-constant-time comparison, the signature logging, and the unguarded index/DoS path. DevOps 'rotate netbrain webhook secret' is now MOOT (no secret in code). CommonUtils.hmacWithJava is now unused but left in place as a generic util. If webhooks are needed later, re-add with @Value-injected secret + MessageDigest.isEqual per the original remediation.
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code: inject the webhook secret via @Value, compare HMACs with MessageDigest.isEqual (constant-time), validate header shape, stop logging signatures. Localized controller change.
- **Non-code tasks (handled separately):**
    - Store a high-entropy webhook secret in the secret store and rotate "netbrain"

### SEC-024 — fastjson 1.2.83 (EOL) and EOL/vulnerable frontend production dependencies shipped to production

- **High / Backend** · SOC 2 CC7.1 · Group G16 Dependency upgrades
- **Status:** Open
- **Code estimate:** 1–3 dev-days · **Confidence:** Discuss
- **Code work:** Code: migrate fastjson 1.x off (standardize on Jackson, or fastjson2 with safeMode) — touches every fastjson usage — and bump marked/dompurify/bootstrap/rxjs/core-js. Heavy regression; scope the fastjson migration before committing.
- **Non-code tasks (handled separately):**
    - Re-run SCA after the upgrades

### SEC-025 — No SAST quality gate, SCA, or secret scanning in either CI pipeline

- **High / CI-CD** · SOC 2 CC8.1 · Group G17 CI/CD security & change management
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** CI config: add waitForQualityGate(abort) after Sonar, add SCA (Dependency-Check/npm audit) and a secret scanner (gitleaks) as failing stages in both Jenkinsfiles.
- **Non-code tasks (handled separately):**
    - Enable Dependabot/Renovate on both repos

### SEC-026 — Deployment compose pulls application images from personal Docker Hub namespaces; EOL/unpinned base images; containers run as root

- **High / CI-CD** · SOC 2 CC6.8 · Group G17 CI/CD security & change management
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code/config: declare a non-root USER in both Dockerfiles, pin base images by digest, forbid :latest, point compose at the org registry. Repo-side change is small.
- **Non-code tasks (handled separately):**
    - Stand up an org-owned private registry (ECR) with immutable tags + signing (cosign)
    - Add image scanning (Trivy/Grype) and scope CodeDeploy hooks to least privilege

### SEC-027 — Customer support-ticket PII has no enforced deletion/retention despite a 'delete when resolved' flag; expired tokens never purged

- **High / Backend** · SOC 2 P4.2 · Group G19 Privacy & data lifecycle
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: a scheduled job honouring whenResolvedDeleteData that purges resolved-ticket PII + expired tokens. Code is small once the retention period is set.
- **Non-code tasks (handled separately):**
    - Agree & document the retention/deletion policy (retention periods)

### SEC-028 — Security events not audited: failed logins, authorization denials, logout, and all administrative IAM changes are unrecorded

- **High / Backend** · SOC 2 CC7.2 · Group G14 Audit logging & log integrity
- **Status:** Open
- **Code estimate:** 1–2 dev-days · **Confidence:** Medium
- **Code work:** Code (cross-cutting): a Spring Security AccessDeniedHandler + audit calls for auth failures, logout, account-disabled, and IAM CRUD with before/after. Spread across several controllers; shares the audit mechanism with SEC-067.
- **Non-code tasks (handled separately):**
    - Forward audit events to centralized logging/SIEM with threshold alerting

### SEC-029 — No centralized/tamper-resistant log aggregation; audit log is a mutable JPA entity

- **High / Infra** · SOC 2 CC7.2 · Group G14 Audit logging & log integrity
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: make audit storage append-only — restrict AuditRepository to a save-only interface; add the log4j2 appender config. The rest is infra.
- **Non-code tasks (handled separately):**
    - Add CloudWatch/Firehose appender shipping to an append-only/immutable destination (S3 Object Lock + retention)
    - INSERT-only DB grant (revoke UPDATE/DELETE) on the audit table
    - Alerting on auth-failure/authz-denial events

### SEC-030 — Live Salesforce OAuth access token and customer license keys logged in clear text (production paths)

- **High / Backend** · SOC 2 CC7.2 · Group G13 Logging hygiene & PII redaction
- **Status:** Fixed (2026-07-02, 2caf39a5) — Code: removed cleartext secret logging. SalesForceService no longer logs the Salesforce OAuth access token (replaced 'Access Token: '+token with a non-sensitive 'authentication succeeded' message). TokenValidator no longer logs the raw license key on parse failure; both sinks now log the licenseId (safe customer identifier, for debuggability) + exception class name, never licenseKeyString and not the raw Jackson message which can echo input. Grep confirms no accessToken/licenseKeyString in any log statement; compiles clean. DevOps backlog: scrub historical kc.log files that already captured tokens/keys, and treat any tokens previously logged as exposed (ties into the SEC-001 secret rotation).
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: remove the specific token/license log statements and grep for similar sinks. Trivial.
- **Non-code tasks (handled separately):**
    - Scrub historical logs that already contain tokens

### SEC-031 — No application-level rate limiting / login throttling enables DoS and brute-force (availability)

- **High / Backend** · SOC 2 A1.2 · Group G15 Rate limiting & DoS resilience
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** Medium
- **Code work:** Code: per-IP/per-account rate limiting at the app layer (mostly shared with SEC-019). Marginal if built together.
- **Non-code tasks (handled separately):**
    - nginx limit_req/limit_conn + sane client_max_body_size for non-upload routes (config)
    - Right-size and monitor the Hikari pool

### SEC-061 — No multi-factor authentication (MFA) for any user, including the built-in admin

- **High / Backend** · SOC 2 CC6.1 · Group G6 MFA / strong authentication
- **Status:** Open
- **Code estimate:** 3–5 dev-days · **Confidence:** Discuss
- **Code work:** Code (front+back): a TOTP/WebAuthn enrollment + verification flow, MFA state on the user, step-up auth for sensitive admin ops, and the frontend enrollment/challenge UI. Genuinely multi-component even for one dev. Decide build-in-app vs federate to an IdP first.
- **Non-code tasks (handled separately):**
    - If federating: configure the enterprise IdP (OIDC/SAML) and enforce MFA there

### SEC-062 — Central RBAC permission check uses substring match (contains) instead of equality, allowing privilege over-grant

- **High / Backend** · SOC 2 CC6.3 · Group G5 Authorization & multi-tenancy
- **Status:** Fixed (2026-07-02, d4cc787b) — CustomPermissionEvaluator.hasPrivilege changed from substring match (grantedAuth.getAuthority().contains(permission)) to exact equals(). Verified the model is exact-string RBAC: authorities are SimpleGrantedAuthority(exact permission name) and checks use the same Constants.PERMISSION_* strings, so equals() keeps all legitimate access and removes the substring over-grant. Added CustomPermissionEvaluatorTest (JUnit 5, 3 tests incl. the required denial regression: a substring of a granted authority is denied) - all pass. Also covers the substring-match portion of SEC-036 (its separate 'add method-level authz to write/sync ops' part remains). The other .contains() hit (GitCollaborator) is Set.contains(element) = correct exact membership, not a vuln.
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: change hasPrivilege from contains() to equals()/Set membership + a denial unit test. Small but security-critical. Shared with SEC-036.

### SEC-072 — Custom object-level permission evaluator ignores the target object, defeating per-tenant authorization application-wide

- **High / Backend** · SOC 2 CC6.3 · Group G5 Authorization & multi-tenancy
- **Status:** Fixed (2026-07-02, 077712f1) — Same root issue as SEC-009, at the evaluator level (CustomPermissionEvaluator ignores the target object). Resolved by the same product-owner design decision: KC uses role/permission-level (RBAC) authorization with no per-tenant/object isolation requirement, so object-level tenant checks are intentionally out of scope. The two concrete defects in this finding's evidence are addressed: (1) substring match getAuthority().contains() -> exact equals() in SEC-062 (d4cc787b) with a denial test; (2) the misleading #targert dead target -> explicit null across all @PreAuthorize in SEC-009 (077712f1). No additional code under the RBAC model. If per-tenant isolation is ever required, reimplement the evaluator to load the target by id and verify tenant membership (the 3-arg overload) + add a tenant A-vs-B test matrix. See [[SEC-009]] and [[SEC-062]].
- **Code estimate:** 1–2 dev-days · **Confidence:** Medium
- **Code work:** Code: reimplement CustomPermissionEvaluator to load the target by id and verify it belongs to the principal's tenant set, use exact permission equality, reject null/unbound targets. The keystone of app-wide per-tenant authz; add a tenant A-vs-B test matrix.

### SEC-073 — Per-license resource/package endpoints authorize on coarse permission only, allowing cross-tenant package and resource download

- **High / Backend** · SOC 2 CC6.3 · Group G5 Authorization & multi-tenancy
- **Status:** Open
- **Code estimate:** 1–1.5 dev-days · **Confidence:** Medium
- **Code work:** Code: bind caller identity to the licenseId being acted on (principal-derived or a per-token license claim), reject mismatches, scope service tokens per license, fix the #targert SpEL. Part of the authz program.

### SEC-074 — Deployment logs and download links retrievable by any authenticated caller via unscoped deploymentId (cross-tenant disclosure)

- **High / Backend** · SOC 2 CC6.3 · Group G5 Authorization & multi-tenancy
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** High
- **Code work:** Code: add a tenant-scoped finder (findByIdAndCustomerIEInstanceIdIn) and authorize the deployment's owning customer before returning logs/links. Bounded once SEC-072 exists.

### SEC-076 — No enforced peer review on code changes: no CODEOWNERS, no/empty PR template, branch protection unconfigured

- **High / CI-CD** · SOC 2 CC8.1 · Group G17 CI/CD security & change management
- **Status:** Fixed (2026-07-02, 157d4fef) — Peer-review enforcement (CC8.1). Branch protection (require PR + non-author approval) enabled at the repo level per the team - this satisfies the core CC8.1 control. Added real PR templates with a review + security checklist to both repos: kc-backend (157d4fef, soc2-prep) and kc-web (e95b533, soc2-prep branch; replaces the 22-byte stub). CODEOWNERS intentionally skipped for now (branch protection already requires a non-author review); can be added later for path-based routing of security-sensitive files. DevOps evidence task remaining: export and retain the branch-protection ruleset for both repos as SOC2 audit evidence.
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code: add a CODEOWNERS file and a real PR template to backend, replace the web stub with a checklist. Small repo files.
- **Non-code tasks (handled separately):**
    - Enable branch protection on develop/release/master in both repos (require PR + non-author review + status checks + dismiss stale)
    - Retain the exported ruleset as evidence

### SEC-032 — Plaintext DB passwords ('Netbrain!', postgres) hardcoded in docker-compose files and packaged application.properties

- **Medium / Infra** · SOC 2 CC6.1 · Group G2 Config & secret externalization
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Config: replace hardcoded DB passwords in compose/properties with env/Docker-secret indirection; remove packaged defaults.
- **Non-code tasks (handled separately):**
    - Rotate "Netbrain!"
    - Source values from a gitignored .env or AWS Secrets Manager

### SEC-033 — Jasypt master passwords committed next to their ciphertext in test resources/code

- **Medium / Backend** · SOC 2 CC6.1 · Group G1 Secrets removal & rotation
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: remove Jasypt master passwords from test resources, inject from env, delete the dead ticket EncryptorConfig. Small.
- **Non-code tasks (handled separately):**
    - Rotate "erumc323"/"netbrain-kc" if reused outside tests

### SEC-034 — Password complexity enforced only client-side; backend accepts any password on create/update/reset

- **Medium / Backend** · SOC 2 CC6.1 · Group G7 Password & reset-flow security
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code: enforce min-length/complexity on the server in updatePassword/save/update/reset paths. Localized validation.

### SEC-035 — Password-reset flow enables user/email enumeration and tokens are not single-use on verify (PII disclosed on public verify)

- **Medium / Backend** · SOC 2 CC6.1 · Group G7 Password & reset-flow security
- **Status:** Open
- **Code estimate:** 0.5 dev-days · **Confidence:** Medium
- **Code work:** Code: identical generic response from sendResetLink, single-use short-lived tokens, drop email/username from public verify. Touches the reset flow; overlaps SEC-071.

### SEC-036 — Write/sync operations lack method-level authorization; permission check uses substring match instead of equality

- **Medium / Backend** · SOC 2 CC6.3 · Group G5 Authorization & multi-tenancy
- **Status:** Fixed (2026-07-02, d4cc787b) — Two parts. (1) Substring-vs-equality permission check: fixed by the shared CustomPermissionEvaluator contains()->equals() change in SEC-062 (d4cc787b), with denial regression test. (2) 'Write/sync operations lack method-level authorization' (syncRepo /repos/common-repo, syncDeltaChangeByReleaseId /repos/releases/{id}): assessed as a FALSE POSITIVE per product-owner domain knowledge - these PATCH endpoints do not write to the git repo; they only trigger KC's existing scheduled GitHub pull/delta-generation earlier than the interval (a refresh action), so the class-level read permission (PERMISSION_COMMON_R) is the correct/legitimate control. No _RW requirement added. Both concerns resolved; no additional code change beyond SEC-062.
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code: add method-level @PreAuthorize on syncRepo/syncDeltaChange and change hasPrivilege from substring to equals. Shares the equality fix with SEC-062.

### SEC-037 — Systemic missing request-body validation: @RequestBody without @Valid and DTO constraints commented out

- **Medium / Backend** · SOC 2 CC6.1 · Group G11 Input validation
- **Status:** Open
- **Code estimate:** 1–2 dev-days · **Confidence:** Medium
- **Code work:** Code: add @Valid + dedicated request DTOs with Bean Validation constraints across many controllers, restore the commented-out Ticket constraints. Breadth drives the cost, but each edit is small.

### SEC-038 — Path traversal sinks: Zip Slip in CommonUtils.unzip and uploaded-filename path in framework-release upload

- **Medium / Backend** · SOC 2 CC6.1 · Group G10 Injection fixes
- **Status:** Open
- **Code estimate:** 0.5 dev-days · **Confidence:** High
- **Code work:** Code: sanitize uploaded filenames (FilenameUtils.getName / server-generated name) and canonicalize+contain each unzip entry. Two well-defined sinks.

### SEC-039 — Custom SMTP send path enables TLS without enforcing certificate validation or requiring STARTTLS

- **Medium / Backend** · SOC 2 CC6.7 · Group G8 Encryption in transit (TLS)
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code/config: set mail.smtp.starttls.required + ssl.checkserveridentity on the custom SMTP path. Property-level.

### SEC-040 — Package signing only covers the first N bytes of the upgrade package (truncated integrity)

- **Medium / Backend** · SOC 2 CC6.1 · Group G12 Crypto at rest & weak crypto
- **Status:** Open
- **Code estimate:** 0.5 dev-days · **Confidence:** Medium
- **Code work:** Code: hash over the entire package (stream the full file) and fix both the byte[] and File overloads, or hard-fail oversized packages. Regression-test the upgrade verify path.

### SEC-041 — Unauthenticated actuator, API-docs/swagger, and webhook endpoints removed from the security filter chain (web.ignoring + permitAll)

- **Medium / Backend** · SOC 2 CC6.1 · Group G20 Misc hardening
- **Status:** Open
- **Code estimate:** 0.5 dev-days · **Confidence:** High
- **Code work:** Code: replace web.ignoring() with permitAll() inside authorizeHttpRequests, tighten the path list, gate /webhooks behind signature verification (SEC-023).
- **Non-code tasks (handled separately):**
    - Disable springdoc/swagger in internet-reachable non-prod (config/env)

### SEC-042 — Server-side root-cause exception messages returned to clients; verbose stack traces and printStackTrace() in production

- **Medium / Backend** · SOC 2 CC7.2 · Group G13 Logging hygiene & PII redaction
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** High
- **Code work:** Code: generic 5xx message + correlation id, replace printStackTrace with logger.error, route frontend logging through the env-gated LogService, add a no-console lint rule. A few central spots.

### SEC-043 — Audited data-export/download actions attributed to licenseId/robot instead of the authenticated user; UI reads not audited

- **Medium / Backend** · SOC 2 CC7.2 · Group G14 Audit logging & log integrity
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: attribute audited export/read to the authenticated UserEntity (Actor.getActor()) instead of licenseId/robot, cover UI reads, null-guard getActor(), add an MDC correlation id. Overlaps SEC-028.

### SEC-044 — Unbounded 4GB multipart upload limit against a 25-connection pool enables resource-exhaustion DoS

- **Medium / Backend** · SOC 2 CC6.6 · Group G15 Rate limiting & DoS resilience
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Config: lower max-file/request-size, set a spill-to-disk threshold, apply across profiles. Verify the legit package-upload still works.
- **Non-code tasks (handled separately):**
    - nginx client_max_body_size + limit_req (config)

### SEC-045 — S3 object uploads do not specify server-side encryption

- **Medium / Backend** · SOC 2 CC6.1 · Group G12 Crypto at rest & weak crypto
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: set .serverSideEncryption(AWS_KMS) on every PutObjectRequest. A few call sites.
- **Non-code tasks (handled separately):**
    - Bucket policy denying unencrypted PutObject + confirm bucket default encryption

### SEC-046 — Frontend nginx serves no Content-Security-Policy or security response headers (and app filter omits Permissions-Policy/frame-ancestors and skips the management port)

- **Medium / Infra** · SOC 2 CC6.6 · Group G9 Security headers / CORS / CSP
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code/config: add a restrictive CSP + X-Frame-Options/nosniff/Referrer-Policy at nginx and extend the app filter (Permissions-Policy, frame-ancestors, base-uri). CSP-without-unsafe-inline needs iterative tuning so the SPA does not break — that is the time.
- **Non-code tasks (handled separately):**
    - Add HSTS at the TLS-terminating layer

### SEC-047 — GitHub webhook DTO/data fixtures and seed data embed PII-shaped values, reused weak hashes, and a committed Jasypt master password

- **Medium / Backend** · SOC 2 C1.1 · Group G1 Secrets removal & rotation
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code: scrub PII-shaped seed/fixture values, regenerate unique hashes, remove the committed Jasypt master password from data fixtures. Localized data files.

### SEC-048 — Outdated backend libraries shipped to production (gson 2.8.5, httpcore/httpclient 4.x, reactor-core pin, saaj/jaxb) plus unpinned RELEASE metaversion

- **Medium / Backend** · SOC 2 CC7.1 · Group G16 Dependency upgrades
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: upgrade gson, drop the reactor pin, pin the RELEASE metaversion, re-verify the XML stack. HttpClient 5 (if attempted) pushes to the high end.

### SEC-049 — CI build/change-management gaps: tests skipped, unsigned artifacts, npm ci --force, OIDC token in world-readable /tmp, reused credential, empty prod approver

- **Medium / CI-CD** · SOC 2 CC8.1 · Group G17 CI/CD security & change management
- **Status:** Open
- **Code estimate:** 1–2 dev-days · **Confidence:** Medium
- **Code work:** CI config: several distinct Jenkinsfile fixes — gating test stage, remove npm --force, OIDC token to a 0600 mktemp file cleaned in finally, per-env credentials, named prod approver, parameterize account ids. Each small but they add up.
- **Non-code tasks (handled separately):**
    - Artifact signing infra (cosign/KMS) + verify at deploy
    - Distinct per-environment OIDC roles scoped by branch/subject

### SEC-055 — Frontend authorization (route guard + nb-permissions + tenant interceptor) is UI-only and client-trusted

- **Medium / Frontend** · SOC 2 CC6.1 · Group G5 Authorization & multi-tenancy
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** Medium
- **Code work:** Code: remove the misleading no-op TenantDomainInterceptor (or document guards as UX only). The real enforcement is the backend authz work (SEC-009/072).

### SEC-063 — Hardcoded role-id '10000' approver bypass keyed to the all-permission built-in Admin role

- **Medium / Backend** · SOC 2 CC6.3 · Group G5 Authorization & multi-tenancy
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** Medium
- **Code work:** Code: drive approver eligibility from explicit permission grants and remove the magic role-id 10000 check; force first-boot rotation of the seeded admin password (data.sql).

### SEC-064 — Access-token 30-day default (config drift), no idle/session timeout, and no server-side token revocation

- **Medium / Backend** · SOC 2 CC6.1 · Group G4 Token lifecycle & revocation
- **Status:** Open
- **Code estimate:** 1–1.5 dev-days · **Confidence:** Medium
- **Code work:** Code: short consistent access-token TTL across profiles, remove the 30-day in-jar default, add a server-side revocation mechanism (jti denylist or token-version) checked on each request/refresh, and a real logout endpoint. Shares the revocation core with SEC-066.

### SEC-066 — Offboarding/deactivation does not revoke outstanding tokens; deactivated users retain access until token expiry

- **Medium / Backend** · SOC 2 CC6.2 · Group G4 Token lifecycle & revocation
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: on deactivation/deletion revoke outstanding tokens (token-version/denylist) and re-check isInactive on every request including refresh. Marginal if built with SEC-064.

### SEC-068 — Audit-relevant logs roll off the host within ~100 days; no enforced >=1-year retention

- **Medium / Infra** · SOC 2 CC7.2 · Group G14 Audit logging & log integrity
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** Medium
- **Code work:** Code/config: raise/remove the max=100/max=1000 caps and the 10MB trigger; separate the audit stream from operational logs. The long-term archive is infra.
- **Non-code tasks (handled separately):**
    - Define & enforce a >=1-year retention for audit logs / audit_log
    - Archive audit_log on a >=1-year schedule to the immutable destination (with SEC-029)

### SEC-069 — Application-tier TLS does not enforce a minimum protocol version or cipher suite (legacy TLS 1.0/1.1 negotiable)

- **Medium / Backend** · SOC 2 CC6.7 · Group G8 Encryption in transit (TLS)
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Config: set server.ssl.enabled-protocols=TLSv1.2,1.3 + explicit ciphers across active profiles; raise nginx ssl_protocols. A few files.
- **Non-code tasks (handled separately):**
    - Upgrade the JRE to a current LTS
    - Add a testssl.sh check asserting TLS 1.0/1.1 are refused

### SEC-070 — Customer upgrade-package password stored in cleartext in the database (Deployment.password)

- **Medium / Backend** · SOC 2 CC6.1 · Group G12 Crypto at rest & weak crypto
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: stop persisting the package password in cleartext — store a salted hash for verification, or encrypt the column with a KMS-backed AttributeConverter; purge after link-expiry. Includes migrating existing rows.

### SEC-071 — Password-reset tokens stored unhashed at rest (plaintext bearer secret in DB)

- **Medium / Backend** · SOC 2 CC6.1 · Group G7 Password & reset-flow security
- **Status:** Open
- **Code estimate:** 0.5 dev-days · **Confidence:** High
- **Code work:** Code: store only a SHA-256 hash of a high-entropy reset token, look up by hash, delete after single use. Overlaps SEC-035.

### SEC-075 — Customer (tenant) soft-delete is not enforced on reads and triggers no cascade purge of tenant data

- **Medium / Backend** · SOC 2 C1.2 · Group G5 Authorization & multi-tenancy
- **Status:** Open
- **Code estimate:** 1–2 dev-days · **Confidence:** Medium
- **Code work:** Code: filter isDeleted=false in all customer read paths and provide an audited customer-deletion that cascades to per-customer artifacts. Read-filter is small; the cascade (incl. S3/log blobs) is the bulk.
- **Non-code tasks (handled separately):**
    - Cascade destruction to S3 objects / deployment-log blobs
    - Agree a retention period before hard purge

### SEC-077 — Production deployment approval gates only the artifact upload, not the CodeDeploy deploy; non-release branches deploy with no approval

- **Medium / CI-CD** · SOC 2 CC8.1 · Group G17 CI/CD security & change management
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** CI config: never leave the Jenkins approval submitter empty (named approver group distinct from author); fail non-release branches that try to deploy.
- **Non-code tasks (handled separately):**
    - Add a CodePipeline manual-approval action before CodeDeploy (or a referenced change ticket)
    - Log approver identity per production deployment

### SEC-078 — Deployed artifacts are not traceable to a source commit/version (static SNAPSHOT jar, overwrite S3 keys, no build metadata)

- **Medium / CI-CD** · SOC 2 CC8.1 · Group G17 CI/CD security & change management
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code/CI: include semver/git short-SHA in the jar name + S3 key and embed build metadata via the build-info / git-commit-id plugin; stop using SNAPSHOT for releases.
- **Non-code tasks (handled separately):**
    - Record the deployed version per environment

### SEC-079 — Rollback limited to hard deploy-failure; no immutable previous artifact and no health-based rollback

- **Medium / Infra** · SOC 2 CC8.1 · Group G18 Infra/IaC, IAM & deploy resilience
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** Medium
- **Code work:** CI: use versioned/immutable S3 keys so a prior build can be redeployed. The auto-rollback + runbook is mostly AWS config.
- **Non-code tasks (handled separately):**
    - Enable CodeDeploy alarm/health-based auto-rollback (CloudWatch + minimum-healthy-hosts)
    - Pin container images by digest
    - Document & periodically test a manual rollback runbook

### SEC-080 — Production CodeDeploy deployment group ships with CloudWatch alarms disabled and no availability alerting wired anywhere

- **Medium / Infra** · SOC 2 A1.1 · Group G18 Infra/IaC, IAM & deploy resilience
- **Status:** Open
- **Code estimate:** 0.5 dev-days · **Confidence:** Medium
- **Code work:** Code: configure Actuator + Micrometer to export metrics (Prometheus/CloudWatch registry). The alarms/SNS/SLOs are AWS + ops config.
- **Non-code tasks (handled separately):**
    - Define CloudWatch alarms (process/5xx/latency/CPU/RDS) wired to SNS/PagerDuty
    - Re-enable AlarmConfiguration on the deployment group
    - Document SLO/SLA thresholds

### SEC-081 — CodeDeploy 'ValidateService' health check only verifies the OS service is registered, not that the app serves traffic

- **Medium / CI-CD** · SOC 2 A1.2 · Group G17 CI/CD security & change management
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code (script): make MonitorService.sh do a real readiness probe (curl the actuator health endpoint) with retry/backoff and fail on non-200; configure actuator readiness/liveness probe groups.

### SEC-083 — CI/CD design document advertises a SonarQube security quality gate the pipelines do not enforce (control-design misrepresentation)

- **Medium / CI-CD** · SOC 2 CC7.1 · Group G17 CI/CD security & change management
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** CI config: add waitForQualityGate(abortPipeline:true) after Sonar on release/PR builds (or correct CI_CD_DESIGN.md). Small; overlaps SEC-025.
- **Non-code tasks (handled separately):**
    - Define the quality gate (fail on new Critical/High vulns) in SonarQube

### SEC-085 — No data-subject access / export / portability capability for personal data (GDPR Art.15/20, CCPA right to know)

- **Medium / Backend** · SOC 2 P5.1 · Group G19 Privacy & data lifecycle
- **Status:** Open
- **Code estimate:** 1–2 dev-days · **Confidence:** Discuss
- **Code work:** Code (after process design): an authenticated export endpoint aggregating all PII for a subject (account, ticket PII, customer-contact PII, audit refs) to JSON, covering ticket contacts who are not users. Needs the DSAR intake/identity-verification workflow defined first.
- **Non-code tasks (handled separately):**
    - Define the DSAR intake + identity-verification process (policy)

### SEC-086 — User-account erasure is incomplete: deletion leaves orphaned PII-linked reset-password tokens and propagates no downstream deletion

- **Medium / Backend** · SOC 2 P4.3 · Group G19 Privacy & data lifecycle
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: make user deletion transactional — purge reset_password_token rows (deleteByUserId / ON DELETE CASCADE / orphanRemoval), pseudonymise audit/actor identity. Overlaps SEC-071/085.

### SEC-087 — No consent capture or privacy notice at any personal-data collection point (operator accounts and support-ticket contacts)

- **Medium / Backend** · SOC 2 P3.1 · Group G19 Privacy & data lifecycle
- **Status:** Open
- **Code estimate:** 1–2 dev-days · **Confidence:** Discuss
- **Code work:** Code (front+back): show a privacy notice and capture consent at PII collection points (ticket submission, account provisioning), persist consent records (subject/purpose/notice-version/timestamp) in a new table, make exportable. Needs the lawful-basis/notice content decided first.
- **Non-code tasks (handled separately):**
    - Decide lawful basis + draft privacy-notice content (legal)

### SEC-088 — No data-classification or sensitivity-labeling scheme for confidential assets (license keys, ticket PII, customer network diagnostics)

- **Medium / Backend** · SOC 2 C1.1 · Group G19 Privacy & data lifecycle
- **Status:** Open
- **Code estimate:** 1–2 dev-days · **Confidence:** Discuss
- **Code work:** Code (after policy): annotate confidential entities/columns (e.g. @Confidential), tag S3 objects, and drive log-masking from the classification. The org classification policy itself must exist first; rollout breadth is the variable.
- **Non-code tasks (handled separately):**
    - Define the org data-classification policy (Public/Internal/Confidential/Restricted)

### SEC-089 — Soft-delete-only for confidential customer (IE-instance) data with no retention-based purge job - data retained indefinitely

- **Medium / Backend** · SOC 2 C1.2 · Group G19 Privacy & data lifecycle
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: a scheduled purge that hard-deletes/anonymizes soft-deleted customer (IE-instance) data + dependents after the retention window, cascading to S3; record destruction in the audit log. Overlaps SEC-075/027.
- **Non-code tasks (handled separately):**
    - Agree the retention schedule (policy)

### SEC-090 — No secure destruction lifecycle for confidential attachments in S3/ShareFile; ShareFile deleteItem is dead code, no S3 lifecycle/object-lock

- **Medium / Backend** · SOC 2 C1.2 · Group G19 Privacy & data lifecycle
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: wire the destruction workflow — call ShareFile deleteItem + S3 deleteObject for ticket attachments on resolve/retention; remove or integrate the dead deleteItem path; audit destructions.
- **Non-code tasks (handled separately):**
    - Configure S3 lifecycle expiration + object-lock/versioning on the confidential bucket

### SEC-067 — User/role/permission CRUD endpoints emit zero audit records (privileged actions unaudited)

- **Medium / Backend** · SOC 2 CC7.2 · Group G14 Audit logging & log integrity
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code: audit every user/role/permission CRUD with actor/target/before-after, ideally by activating the dormant @Auditable aspect. Shares the audit mechanism with SEC-028.
- **Non-code tasks (handled separately):**
    - Route records to the immutable audit destination (SEC-029)

### SEC-050 — ShareFile getItemByPath builds URL with unencoded user path (URL/parameter injection)

- **Low / Backend** · SOC 2 CC6.1 · Group G10 Injection fixes
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: URL-encode the dynamic path in the ShareFile getItemByPath call + pattern-validate. Trivial.

### SEC-051 — Templated support email injects unescaped user-controlled values (HTML/email content injection)

- **Low / Backend** · SOC 2 CC6.1 · Group G10 Injection fixes
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: enable FreeMarker HTML auto-escaping (output_format=HTML / rename to .ftlh) so all ${} are escaped. Small.

### SEC-052 — Weak/legacy cryptography: MD5 file hashing and hand-rolled RSA license signature verification

- **Low / Backend** · SOC 2 CC6.1 · Group G12 Crypto at rest & weak crypto
- **Status:** Open
- **Code estimate:** 0.5 dev-days · **Confidence:** Medium
- **Code work:** Code: replace MD5 with SHA-256 in the helpers and use Signature.getInstance("SHA256withRSA").verify() for license checks instead of manual Cipher+Arrays.equals. Confirm existing licenses still validate.

### SEC-053 — RSA license-verification public key embedded in committed config template alongside live secrets

- **Low / Backend** · SOC 2 CC6.1 · Group G2 Config & secret externalization
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: move license.key out of the committed config template into the Secrets Manager bundle. No rotation emergency. Small move.

### SEC-054 — Frontend defense-in-depth gaps: bearer token attached to any host, open-redirect returnUrl, untrusted content via [innerHTML], guid() uses Math.random()

- **Low / Frontend** · SOC 2 CC6.1 · Group G20 Misc hardening
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** Medium
- **Code work:** Code (frontend): bundle of small fixes — origin-check before attaching the bearer, same-origin returnUrl validation, render text instead of [innerHTML], crypto.randomUUID for guid(). Several small edits.

### SEC-056 — PII (email/displayName) and customer license data written to local application logs

- **Low / Backend** · SOC 2 C1.1 · Group G13 Logging hygiene & PII redaction
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** High
- **Code work:** Code: log opaque ids (user id, hashed license id) instead of email/displayName/full customer JSON; apply layout redaction. Localized.

### SEC-057 — Vulnerable/EOL frontend test & build tooling and transitive dev dependencies

- **Low / Frontend** · SOC 2 CC7.1 · Group G16 Dependency upgrades
- **Status:** Open
- **Code estimate:** 1–3 dev-days · **Confidence:** Medium
- **Code work:** Code: migrate e2e protractor -> Playwright/Cypress (the bulk of the work), drop babel6/Storybook4/TSLint tooling, add package.json overrides for transitives. The e2e migration drives this.

### SEC-058 — Very old test-scope libraries pinned across modules (Mockito 2.23.4; nb-kc-main JUnit 4.12 = CVE-2020-15250)

- **Low / Backend** · SOC 2 CC7.1 · Group G16 Dependency upgrades
- **Status:** Open
- **Code estimate:** 0.5–1 dev-days · **Confidence:** High
- **Code work:** Code: upgrade Mockito to 5.x and JUnit to Jupiter 5 (or >=4.13.2), drop per-module version pins. May need some test-code fixes.

### SEC-082 — Application, frontend, and reverse-proxy containers have no restart policy and no HEALTHCHECK (no runtime self-healing)

- **Low / Infra** · SOC 2 A1.2 · Group G18 Infra/IaC, IAM & deploy resilience
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Config: add restart:unless-stopped to the three services in compose and a HEALTHCHECK to each Dockerfile. Trivial.
- **Non-code tasks (handled separately):**
    - Optionally run >=2 replicas behind a load balancer (infra)

### SEC-084 — SonarQube scanner configuration neuters its own SAST coverage: Java-binaries-only, no sonar.sources, excludes resources/ (where committed secrets live)

- **Low / CI-CD** · SOC 2 CC7.1 · Group G17 CI/CD security & change management
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** CI config: remove **/resources/** from sonar.exclusions, set sonar.sources, enable secret/security-hotspot rules, run on release/PR. Config-file edit; overlaps SEC-025/083.

### SEC-065 — Unwired license-key authentication provider has a no-op credential check (isValidLicense hardcoded true)

- **Low / Backend** · SOC 2 CC6.1 · Group G20 Misc hardening
- **Status:** Open
- **Code estimate:** 0.25–0.5 dev-days · **Confidence:** Medium
- **Code work:** Code: remove the unwired stubbed license-key auth path (provider + filter) if unused, or implement real validation mirroring LicenseValidator.validate before wiring it; add a rejection test. Decision + small implementation.

### SEC-059 — Inconsistent/undocumented mTLS boundary configuration across profiles

- **Info / Backend** · SOC 2 CC6.7 · Group G8 Encryption in transit (TLS)
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** Discuss
- **Code work:** Config (after a decision): remove the divergent/commented client-auth settings and make profiles consistent. The work is the architecture decision, not the code.
- **Non-code tasks (handled separately):**
    - Decide & document whether mTLS is required at the app or proxy boundary

### SEC-060 — Trivial credential-like literals and onboarding screenshots committed (informational; no live secrets)

- **Info / Backend** · SOC 2 CC6.1 · Group G1 Secrets removal & rotation
- **Status:** Open
- **Code estimate:** 0.25 dev-days · **Confidence:** High
- **Code work:** Code: remove the onboarding screenshots/test literals from the repo. Trivial.
- **Non-code tasks (handled separately):**
    - Move dev-setup docs to an internal wiki; standardize dev AWS access on SSO/OIDC

## DevOps / non-code backlog

72 tasks that are not developer code work — route to DevOps / platform / security-governance owners.

| Finding | Code status | Non-code task |
|---|---|---|
| SEC-001 | Fixed | Generate a >=256-bit key per environment and store it in AWS Secrets Manager |
| SEC-001 | Fixed | Rotate the key and invalidate existing tokens (coordinated redeploy) |
| SEC-001 | Fixed | Purge the literal from git history |
| SEC-002 | Fixed | Revoke & rotate the Git PAT and the recoverable git password (treat as compromised) |
| SEC-002 | Fixed | Purge both from git history (BFG/filter-repo), force-push, all devs re-clone |
| SEC-002 | Fixed | Inject git auth at runtime from Secrets Manager |
| SEC-003 | Fixed | Rotate the TLS cert/keypair and DER key (assume compromised), set a strong keystore password |
| SEC-003 | Fixed | Purge the files + password from git history |
| SEC-003 | Fixed | Provision the keystore/password at deploy time from Secrets Manager |
| SEC-004 | Fixed | Rotate the ShareFile account password and OAuth client secret (needs ShareFile admin) |
| SEC-004 | Fixed | Purge from git history |
| SEC-006 | Fixed | Import the internal/private CA into the JVM/dedicated truststore on the hosts |
| SEC-010 | Open | Decide & provision per-service credentials or mTLS for service identities |
| SEC-012 | Open | Deliver runtime config/keystore via Secrets Manager/SSM at boot |
| SEC-012 | Open | Rotate key material once history is purged |
| SEC-013 | Open | Add an IAM linter (cfn-nag/checkov) to the pipeline |
| SEC-013 | Open | Validate the scoped policy in a real deploy |
| SEC-014 | Open | Provision valid TLS certs and decide the production TLS-termination point |
| SEC-015 | Fixed | Place the RDS CA bundle on the hosts |
| SEC-016 | Open | Re-encrypt all stored secrets across environments and coordinate the rollout |
| SEC-018 | Fixed | Bind the management port to localhost or drop the 8081 host mapping (compose) |
| SEC-019 | Open | Optionally add nginx limit_req/limit_conn (config) |
| SEC-023 | Fixed | Store a high-entropy webhook secret in the secret store and rotate "netbrain" |
| SEC-024 | Open | Re-run SCA after the upgrades |
| SEC-025 | Open | Enable Dependabot/Renovate on both repos |
| SEC-026 | Open | Stand up an org-owned private registry (ECR) with immutable tags + signing (cosign) |
| SEC-026 | Open | Add image scanning (Trivy/Grype) and scope CodeDeploy hooks to least privilege |
| SEC-027 | Open | Agree & document the retention/deletion policy (retention periods) |
| SEC-028 | Open | Forward audit events to centralized logging/SIEM with threshold alerting |
| SEC-029 | Open | Add CloudWatch/Firehose appender shipping to an append-only/immutable destination (S3 Object Lock + retention) |
| SEC-029 | Open | INSERT-only DB grant (revoke UPDATE/DELETE) on the audit table |
| SEC-029 | Open | Alerting on auth-failure/authz-denial events |
| SEC-030 | Fixed | Scrub historical logs that already contain tokens |
| SEC-031 | Open | nginx limit_req/limit_conn + sane client_max_body_size for non-upload routes (config) |
| SEC-031 | Open | Right-size and monitor the Hikari pool |
| SEC-061 | Open | If federating: configure the enterprise IdP (OIDC/SAML) and enforce MFA there |
| SEC-076 | Fixed | Enable branch protection on develop/release/master in both repos (require PR + non-author review + status checks + dismiss stale) |
| SEC-076 | Fixed | Retain the exported ruleset as evidence |
| SEC-032 | Open | Rotate "Netbrain!" |
| SEC-032 | Open | Source values from a gitignored .env or AWS Secrets Manager |
| SEC-033 | Open | Rotate "erumc323"/"netbrain-kc" if reused outside tests |
| SEC-041 | Open | Disable springdoc/swagger in internet-reachable non-prod (config/env) |
| SEC-044 | Open | nginx client_max_body_size + limit_req (config) |
| SEC-045 | Open | Bucket policy denying unencrypted PutObject + confirm bucket default encryption |
| SEC-046 | Open | Add HSTS at the TLS-terminating layer |
| SEC-049 | Open | Artifact signing infra (cosign/KMS) + verify at deploy |
| SEC-049 | Open | Distinct per-environment OIDC roles scoped by branch/subject |
| SEC-068 | Open | Define & enforce a >=1-year retention for audit logs / audit_log |
| SEC-068 | Open | Archive audit_log on a >=1-year schedule to the immutable destination (with SEC-029) |
| SEC-069 | Open | Upgrade the JRE to a current LTS |
| SEC-069 | Open | Add a testssl.sh check asserting TLS 1.0/1.1 are refused |
| SEC-075 | Open | Cascade destruction to S3 objects / deployment-log blobs |
| SEC-075 | Open | Agree a retention period before hard purge |
| SEC-077 | Open | Add a CodePipeline manual-approval action before CodeDeploy (or a referenced change ticket) |
| SEC-077 | Open | Log approver identity per production deployment |
| SEC-078 | Open | Record the deployed version per environment |
| SEC-079 | Open | Enable CodeDeploy alarm/health-based auto-rollback (CloudWatch + minimum-healthy-hosts) |
| SEC-079 | Open | Pin container images by digest |
| SEC-079 | Open | Document & periodically test a manual rollback runbook |
| SEC-080 | Open | Define CloudWatch alarms (process/5xx/latency/CPU/RDS) wired to SNS/PagerDuty |
| SEC-080 | Open | Re-enable AlarmConfiguration on the deployment group |
| SEC-080 | Open | Document SLO/SLA thresholds |
| SEC-083 | Open | Define the quality gate (fail on new Critical/High vulns) in SonarQube |
| SEC-085 | Open | Define the DSAR intake + identity-verification process (policy) |
| SEC-087 | Open | Decide lawful basis + draft privacy-notice content (legal) |
| SEC-088 | Open | Define the org data-classification policy (Public/Internal/Confidential/Restricted) |
| SEC-089 | Open | Agree the retention schedule (policy) |
| SEC-090 | Open | Configure S3 lifecycle expiration + object-lock/versioning on the confidential bucket |
| SEC-067 | Open | Route records to the immutable audit destination (SEC-029) |
| SEC-082 | Open | Optionally run >=2 replicas behind a load balancer (infra) |
| SEC-059 | Open | Decide & document whether mTLS is required at the app or proxy boundary |
| SEC-060 | Open | Move dev-setup docs to an internal wiki; standardize dev AWS access on SSO/OIDC |

